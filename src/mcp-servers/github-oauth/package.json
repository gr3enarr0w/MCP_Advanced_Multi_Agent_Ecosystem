{
  "name": "github-oauth-mcp-server",
  "version": "1.0.0",
  "description": "OAuth2 GitHub MCP Server for Model Context Protocol",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:unit": "jest --testPathPattern=unit",
    "test:integration": "jest --testPathPattern=integration",
    "lint": "eslint *.js tests/**/*.js"
  },
  "keywords": [
    "mcp",
    "github",
    "oauth",
    "model-context-protocol"
  ],
  "author": "MCP User",
  "license": "MIT",
  "dependencies": {
    "@octokit/rest": "^20.0.0",
    "express": "^4.18.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "eslint": "^9.9.0"
  }
}
